pipeline {
   agent { label 'kopserver'
}
   stages {
      stage('git checkout') {
         steps {
            git 'https://github.com/shaikshareefmastan/apipro.git'
         }
       }
      stage('Build application') {
         steps {
	            sh "pwd"
                    sh "ls -la"
                 }
               }
   }
}
