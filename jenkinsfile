pipeline {
   agent { label 'kopserver'
}
   stages {
      stage('git checkout') {
         steps {
            git 'https://github.com/shaikshareefmastan/apipro.git'
         }
       }
      stage('Build application') {
         steps {
            dir('/home/ec2-user/jenkins/workspace/apipro_pipeline'){
	            sh "pwd"
                    sh "ls -la"
                    sh "cp collection.json /tmp"
                }
                 }

               }
   }
}
